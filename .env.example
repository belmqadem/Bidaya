# .env.example — Exemple de configuration (ne contient pas de secrets)
# Copiez en `.env` et remplacez les valeurs par vos clés réelles.
# IMPORTANT: ne commitez jamais le fichier `.env`.

DATABASE_URL="postgresql://user:password@host:5432/dbname?sslmode=require"

# MiniMax (LLM)
MINIMAX_API_KEY="your-minimax-api-key"
MINIMAX_BASE_URL="https://api.minimax.io/v1/chat/completions"
MINIMAX_MODEL="MiniMax-M2.5"

# ElevenLabs (speech-to-text)
ELEVENLABS_API_KEY="your-elevenlabs-api-key"

# Environnement
NODE_ENV=development
